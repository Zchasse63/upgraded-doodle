# tsg-cc-bridge — Audit & Plan to 100%

Snapshot taken 2026-05-11 after PR 1 + PR 2 shipped and the Glofox `model:"event"` booking fix landed. Edge Function `pushpress-webhook` is at **v13**, deployed live, receiving real PushPress webhooks against TSG's sandbox PushPress (company `client_ddd1caa8be7225`).

---

## 1. What's built (verified working)

### Foundation — 100%
| Area | Status | Where |
|---|---|---|
| HMAC-SHA256 signature verification, constant-time, hex parse rejection | ✓ | `supabase/functions/_shared/signature.ts` |
| Idempotency (dedup_key = SHA-256 of `event\|id\|companyId\|created`) | ✓ | `supabase/functions/_shared/dedup.ts` |
| `event_log` insert-pending + update-final pattern, 23505 dedup handling | ✓ | `supabase/functions/_shared/event-log.ts` |
| Q9 sauna filter (`SAUNA_CLASS_TYPE_ALLOWLIST` + `SAUNA_PLAN_CATEGORY_ALLOWLIST`) | ✓ | `supabase/functions/_shared/filter.ts` |
| Body-size cap, JWT-off, top-level `Deno.serve` | ✓ | `supabase/functions/pushpress-webhook/index.ts` |

### Clients & mappings — 100%
| Area | Status | Where |
|---|---|---|
| Glofox 3-header auth, rate-limit pacing, 200+success:false detection, `model:"event"` fix | ✓ | `supabase/functions/_shared/glofox-client.ts` |
| Glofox client modes: `mock` / `readonly` / `live` (3 concrete classes) | ✓ | same file |
| PushPress REST: `getCustomer`, `getClass`, `getPlan` | ✓ | `supabase/functions/_shared/pushpress-client.ts` |
| Lazy slot mapping with time-window defense-in-depth | ✓ | `supabase/functions/_shared/mappings.ts` |
| Member link with email match + auto-create-lead via `/2.0/register` | ✓ | same file |
| Pending refund enqueue (idempotent) | ✓ | same file |
| Data-driven `plan_mappings` (no code change for new plans) | ✓ | `supabase/migrations/0005_plan_mappings_seed.sql` |

### Webhook handlers — 2 of 9 implemented (22%)
| Event | Status |
|---|---|
| `reservation.created` | ✓ end-to-end live |
| `enrollment.created` | ✓ end-to-end live (4 NOEQL memberships assigned) |
| `enrollment.status.changed` | ✗ stubbed (returns `skipped`) |
| `enrollment.deleted` | ✗ stubbed |
| `reservation.canceled` | ✗ stubbed |
| `reservation.waitlisted` | ✗ stubbed |
| `checkin.created` | ✗ stubbed |
| `class.canceled` | ✗ stubbed |
| `customer.details.changed` | ✗ stubbed |

### Tooling & ops — 100% (for current scope)
| Tool | Status |
|---|---|
| Reconciliation script (`--mode reservations\|enrollments\|all`, `--only`, `--days`, `--replay`) | ✓ |
| One-shot webhook subscription | ✓ |
| Local test-driver | ✓ |
| Fixture signature generator | ✓ |

### Tests, deploy, live state
- **53 tests passing** (signature 14, dedup 4, filter 13, integration 14, enrollment-created 8)
- Edge Function v13 deployed at `https://pygbvcqjpwfodmoqkhos.supabase.co/functions/v1/pushpress-webhook`
- PushPress webhook subscription active — real webhooks delivered
- `GLOFOX_MODE=live` — real Glofox writes happening
- 5 migrations applied

---

## 2. What's remaining

### A. The 7 missing handlers (PR 3 — bulk of remaining code)

| # | Handler | Approach |
|---|---|---|
| A1 | `reservation.canceled` | Look up Glofox `booking_id` from prior `reservation.created` event_log row → `DELETE /2.3/.../bookings/{id}`. Idempotent (404 = success). |
| A2 | `class.canceled` | Fan-out — find every prior `reservation.created` for this class, DELETE each Glofox booking. Rate-limit aware. |
| A3 | `pushpress_enrollment_links` table + backfill | Stores `userMembershipId` per PushPress enrollment so cancel handlers can find it. Backfill from existing 4 success rows. |
| A4 | `enrollment.status.changed` | Cancel via `POST /v3.0/memberships/{userMembershipId}/cancel`. Other transitions log+ack. |
| A5 | `enrollment.deleted` | Same cancel path as A4 (slim `{id}` payload). |
| A6 | `checkin.created` | Skip unless `kind=class && role=attendee && result=success`. `POST /2.0/attendances`. |
| A7 | `customer.details.changed` | `PUT /2.0/members/{userId}`. No debouncing (v2). |
| A8 | `reservation.waitlisted` | `POST /2.3/.../bookings` with waitlist flag — needs Q4 verification first. |

### B. Open questions that block hardening

| Q | What | Resolution path |
|---|---|---|
| Q4 | Glofox capacity-full response shape (current regex guess) | Manually book a 13th user against a 12-cap event, capture body, replace regex |
| DQ7 | Glofox `purchaseMembership` didn't return `userMembershipId` on our test | Add lookup-by-user fallback in cancel handlers; A3 table records it when possible |
| Q2 | PushPress retry policy (undocumented) | Ask PushPress support — not blocking |
| Q10 | Replay-staleness warning on 24h+ dedup hits | Easy to add; observability nicety |
| Q11 | `event_log.payload` retention (indefinite) | Add pgcron TTL job |

### C. Data / operational gaps (not code)

1. 6 stale PushPress test reservations need cancelling in the PushPress UI (no API)
2. Weekend schedule in PushPress (Sat 11am / Sun 12pm Guided Sauna + Cold Plunge) not yet added
3. Migration 0005 says `complimentary`, live DB has `cash` — sync the migration file
4. `SLACK_OPS_WEBHOOK_URL` env var empty — no alerting on failures
5. `CRON_SECRET` placeholder

### D. Production cutover (final step)

- Get CC's PushPress `API-KEY` + `company-id`
- Run `setup-webhook.ts` against CC's PushPress → capture new `signingSecret`
- Update 3 Supabase secrets (`PUSHPRESS_API_KEY`, `PUSHPRESS_COMPANY_ID`, `PUSHPRESS_WEBHOOK_SIGNING_SECRET`)
- Pre-cutover `reconcile.ts --mode all --replay` to bootstrap CC sauna members + reservations
- Decommission TSG-sandbox subscription
- Monitor `event_log` for 24h

---

## 3. Plan to 100%

### Milestone A — PR 3 handlers (largest chunk)
Order: cancel-related first → attendance → profile → waitlist (after Q4)

### Milestone B — Hardening
Resolve Q4 + DQ7 + sync 0005 migration

### Milestone C — Production-readiness
Slack alerts + replay-staleness + retention + scheduled reconcile

### Milestone D — Cutover
Switch from TSG sandbox to CC's real PushPress (data/credentials, not code)

---

## TL;DR

**Built:** Full foundation, 2 of 9 handlers, 53 tests, deployed live and receiving real webhooks.

**Remaining:** 7 handlers (PR 3), 2 API-shape verifications (Q4, DQ7), ops wiring (Slack, retention, scheduled reconcile), then cutover.
